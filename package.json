{
  "name": "cinema-tickets-javascript",
  "description": "TicketService implementation for DWP coding exercise (Node.js).",
  "version": "1.0.1",
  "engines": {
    "node": ">=20.9.0"
  },
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
